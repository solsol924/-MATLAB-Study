<<<<<<< HEAD
# MATLAB 기초 실습 자료

MATLAB의 기본 개념과 행렬 연산을 배우기 위한 실습 자료입니다.

## 📋 목차

1. [명령 창 사용하기](#1-명령-창-사용하기)
2. [스칼라, 벡터, 행렬 생성하기](#2-스칼라-벡터-행렬-생성하기)
3. [행렬 계산하기](#3-행렬-계산하기)
4. [행렬 인덱싱과 편집](#4-행렬-인덱싱과-편집)
5. [함수](#5-함수)
6. [구조 프로그래밍](#6-구조-프로그래밍)

## 🎯 학습 목표

- MATLAB 명령 창의 기본 사용법 이해
- 벡터와 행렬 생성 및 조작
- 행렬 연산(내적, 외적, 원소별 연산) 수행
- 인덱싱을 통한 행렬 요소 접근 및 수정
- 내장 함수 활용 및 사용자 정의 함수 작성
- 조건문과 반복문을 사용한 프로그래밍

## 📦 필요 환경

- MATLAB R2019b 이상 권장
- Live Script를 실행할 수 있는 MATLAB 설치 필요

## 🚀 시작하기

### 실습 파일 다운로드

```bash
git clone https://github.com/hymmni/matlab-tutorials.git
cd matlab-tutorials
```


### 실습 파일 열기

1. MATLAB을 실행합니다
2. 다운로드한 폴더로 이동합니다
3. `matlab_basics.mlx` 파일을 더블클릭하여 엽니다

## 📚 실습 내용

### 1. 명령 창 사용하기

- `pi` 상수 출력 및 포맷 변경 (`format long`, `format short`)
- 기본 연산 (`*`, `^`)
- 변수 확인 (`who`, `whos`)

### 2. 스칼라, 벡터, 행렬 생성하기

**스칼라**
- 단일 숫자 값 할당

**벡터**
- 행 벡터: `[1 2 3 4 5]`
- 열 벡터: `[1; 2; 3; 4; 5]` 또는 `[1 2 3 4 5]'`
- 등간격 벡터: `1:5`, `1:0.5:3`
- `linspace`, `logspace` 함수 활용

**행렬**
- 일반 행렬: `[1 2 3; 4 5 6; 7 8 9]`
- 특수 행렬: `zeros`, `ones`, `eye`, `diag`

### 3. 행렬 계산하기

**벡터의 곱**
- 내적 (Inner Product): 행벡터 × 열벡터
- 크로스곱 (Cross Product): `cross(a, b)` (3차원 벡터)
- 외적 (Outer Product): 열벡터 × 행벡터

**행렬의 곱**
- 행렬 곱: `A*B`
- 행렬 제곱: `A^2` (정방 행렬)
- 원소별 곱: `A.*B`
- 원소별 제곱: `A.^2`

### 4. 행렬 인덱싱과 편집

**인덱싱 방법**
- 위치 기준 인덱싱: `A(3,2)`, `A(1:3, 2:4)`
- 선형 인덱싱: `A(6)`
- 논리형 인덱싱

**행렬 편집**
- 요소 수정: `A(2,3) = 100`
- 행/열 수정: `A(1,:) = [10 20 30]`
- 행렬 결합: `[A B]`, `[A; B]`

### 5. 함수

**내장 함수**
- 수학 함수: `log`, `log10`, `sqrt`, `round`, `ceil`, `floor`
- 통계 함수: `max`, `min`, `mean`, `sum`, `prod`

**입출력 함수**
- `disp`: 간단한 출력
- `fprintf`: 형식화된 출력
- `input`: 사용자 입력

### 6. 구조 프로그래밍

**스크립트**
- `.m` 파일로 여러 명령을 순차 실행

**조건문**
- `if-elseif-else-end` 구조
- 학점 계산 예제
- 에러 처리: `error()` vs `disp()`

**반복문**
- `for` 문: 정해진 횟수 반복
- `while` 문: 조건 기반 반복
- 제어 키워드: `break`, `continue`

**사용자 정의 함수**
- 기본 함수 작성
- 부함수 (Subfunction)
- 무명 함수 (Anonymous Function): `@(x) x^2`
- 함수 핸들 전달

## 💡 실습 팁

1. **직접 타이핑하기**: 코드를 복사하지 말고 직접 타이핑하면서 익숙해지세요
2. **help 명령어 활용**: 궁금한 함수가 있으면 `help 함수명`을 입력하세요
3. **에러 메시지 읽기**: 에러가 발생하면 메시지를 꼼꼼히 읽고 원인을 파악하세요
4. **Workspace 확인**: 변수들의 상태를 수시로 확인하세요

## 🔍 주요 개념 요약

| 개념 | 설명 | 예시 |
|------|------|------|
| 행렬 곱 `*` | 선형대수 행렬 곱셈 | `A*B` |
| 원소별 곱 `.*` | 같은 위치 원소끼리 곱셈 | `A.*B` |
| 전치 `'` | 행과 열 바꾸기 | `A'` |
| 콜론 `:` | 범위 지정 또는 전체 선택 | `1:5`, `A(:,2)` |
| `end` | 마지막 인덱스 | `A(end, :)` |

## 🤔 자주 묻는 질문

**Q: MATLAB의 인덱스는 0부터 시작하나요?**  
A: 아니요, MATLAB은 1부터 시작합니다. (C/Python과 다름)

**Q: `A*B`와 `A.*B`의 차이는 무엇인가요?**  
A: `A*B`는 선형대수 행렬 곱셈, `A.*B`는 같은 위치의 원소끼리 곱하는 연산입니다.

**Q: 세미콜론(`;`)은 언제 사용하나요?**  
A: 1) 명령 끝에 붙여서 출력 억제, 2) 행렬에서 행 구분, 3) 벡터에서 열 구분

## 📖 참고 자료

- [MATLAB 공식 문서](https://www.mathworks.com/help/matlab/)
- [MATLAB Onramp (무료 온라인 코스)](https://www.mathworks.com/learn/tutorials/matlab-onramp.html)
- [MATLAB Central (커뮤니티)](https://www.mathworks.com/matlabcentral/)

## 📝 라이선스

이 자료는 교육 목적으로 자유롭게 사용할 수 있습니다.

## 👤 작성자

손희민 (godjedh516@naver.com)

---

**🎓 즐거운 MATLAB 학습 되세요!**
=======
# -MATLAB-Study
MATLAB-Study
>>>>>>> 633c096763d9638da2309de90fe9510c7b3d118c
